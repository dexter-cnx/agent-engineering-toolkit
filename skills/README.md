# skills/ (Legacy Library)

Status: **legacy skill set** retained for compatibility and historical workflows.

Canonical OS runtime model is defined by:
- `agents/` (roles/workflows)
- `prompts/` (runtime prompt model)
- `system/` (contracts/policies)

Use this directory only when explicitly working on legacy skill flows.
