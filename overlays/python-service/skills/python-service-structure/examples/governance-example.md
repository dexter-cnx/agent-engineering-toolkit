# python-service-structure Governance Example

## Example scenario

A Python service needs explicit router, service, repository, and adapter boundaries.

## Example outcome

The skill keeps transport thin and writes stable ownership notes for each service layer.
