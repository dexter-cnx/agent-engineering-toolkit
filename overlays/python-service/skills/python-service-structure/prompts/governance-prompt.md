Follow `AGENTS.md` and `overlays/python-service/AGENTS.overlay.md`.
Use the `python-service-structure` skill contract before implementation.

Task:
Update the consuming Python service with explicit routers, services, repositories, and
adapter boundaries.

Requirements:
- keep routers thin
- keep orchestration in services
- keep persistence behind repositories
- keep external providers behind adapters

Deliver:
1. file plan
2. boundary plan
3. implementation notes
4. verification notes
