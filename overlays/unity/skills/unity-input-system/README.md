# unity-input-system

Covers the Unity New Input System, action maps, rebinding, and device separation.
See `skill.md` for the operating contract and `prompts/`, `checklists/`, and `templates/` for task flow.
