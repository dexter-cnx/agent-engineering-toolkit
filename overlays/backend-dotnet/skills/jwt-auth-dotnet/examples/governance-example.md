# jwt-auth-dotnet Governance Example

## Example scenario

A backend needs JWT issuance and verification rules with explicit claim shape, expiry,
and signing assumptions.

## Example outcome

The skill keeps token logic behind the backend boundary and documents the verification
path before any promotion run.
