Follow `AGENTS.md` and `overlays/backend-dotnet/AGENTS.overlay.md`.
Use the `jwt-auth-dotnet` skill contract before implementation.

Task:
Update the consuming ASP.NET Core repository with governed JWT auth boundaries.

Requirements:
- keep issuance and verification rules explicit
- keep token claims and expiry documented
- keep regression and token-policy checks intact

Deliver:
1. file plan
2. token boundary plan
3. implementation notes
4. verification notes
