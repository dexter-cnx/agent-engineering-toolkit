# server-client-boundaries Governance Example

## Example scenario

A Next.js feature loads initial data on the server but keeps interactive controls on the
client.

## Example outcome

The skill records a clean boundary map and keeps transport logic out of the client split.
