# CSV to JSON generator

Run the generator from the project root:

```bash
python scripts/generate_l10n_from_csv.py
```

It reads `assets/i18n/translations.csv` and writes JSON files into `assets/i18n/generated/`.
