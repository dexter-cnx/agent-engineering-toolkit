# iOS Setup

## Firebase
1. Add `GoogleService-Info.plist` to the Runner target
2. Confirm bundle identifier matches Firebase project settings

## Notifications
- Enable Push Notifications capability
- Enable Background Modes for remote notifications when needed
- Request permission at runtime through your notification service

## Maps
- Add Maps API key through the chosen iOS integration path
- Validate Info.plist entries and deployment target compatibility
