# Prompt: Apply Flutter Web Loading

Upgrade this Flutter project so web startup shows a real loading experience instead of a blank white page.

Requirements:
- add immediate HTML/CSS loader in `web/index.html`
- add `web/style.css`
- use the project's Flutter bootstrap flow cleanly
- keep branding minimal and reusable
- make sure loader disappears when Flutter app is ready
- do not change Dart architecture just to add the loader
- include a short checklist for testing in release mode
