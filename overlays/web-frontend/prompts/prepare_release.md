# Prepare Release

Follow `AGENTS.md` strictly.
Apply `overlays/web-frontend/AGENTS.overlay.md`.

Use `web-deployment`.

Goal:
- verify build and test readiness
- confirm release notes and environment docs are aligned
- confirm deployment steps are explicit
