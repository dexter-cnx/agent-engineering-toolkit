# Implement Auth

Follow `AGENTS.md` strictly.
Apply `overlays/web-frontend/AGENTS.overlay.md`.

Use `web-auth` and any routing or state skills required.

Goal:
- implement authentication flow with clear boundaries
- keep token/session handling behind approved layers
- cover login, logout, and route protection
