# Review Checklist

- page logic does not leak into shared UI
- design-system primitives stay reusable
- state changes happen in the right layer
- API access stays behind services or adapters
- tests match the changed behavior
