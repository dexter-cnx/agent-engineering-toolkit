# Worked Example — Web Frontend Feature

Use the foundation lifecycle, then map it to:
- route/page layer for page orchestration
- feature modules for business-focused UI behavior
- reusable component layer for presentation
- services/state for data access and state management

Verification examples:
```bash
npm run lint
npm run build
```
