# Checklist: tailwind token flutter bridge

- token categories are small and durable
- semantic naming is clear
- Tailwind maps from token-backed values
- Flutter maps the same semantics
- repeated style patterns are extracted
- hard-coded raw values are minimized
- light and dark rules are explicit if supported
