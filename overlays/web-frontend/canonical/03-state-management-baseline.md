# State Management Baseline

- Keep state placement intentional.
- Use local state for ephemeral UI concerns.
- Use shared state only when multiple surfaces need the same data.
- Keep mutations in a dedicated state layer.
