# Worked Example — Flutter Feature

Use the foundation lifecycle, then map it to:
- `lib/presentation/` for UI and state
- `lib/domain/` for use cases and models
- `lib/data/` for repository and datasource work

Verification examples:
```bash
flutter analyze
flutter test
```
