import 'package:flutter/material.dart';

class SampleFeaturePage extends StatelessWidget {
  const SampleFeaturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SampleFeature Page'),
      ),
    );
  }
}
