# Flutter Web Loading

This starter app ships with a simple HTML/CSS loader in `web/`.

## Files
- `web/index.html`
- `web/style.css`
- `web/flutter_bootstrap.js`

## Goal
Show an immediate loading UI before Flutter paints its first frame.

## Test
Run a release build and open it in a browser with cache disabled.
