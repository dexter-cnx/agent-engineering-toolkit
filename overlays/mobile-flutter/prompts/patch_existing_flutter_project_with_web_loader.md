# Prompt: Patch Existing Flutter Project With Web Loader

Apply the standard Flutter Web loader baseline to an existing Flutter project.

Requirements:
- patch `web/index.html`
- add `web/style.css`
- add `web/flutter_bootstrap.js` if needed
- keep Dart application architecture unchanged
- do not overwrite project-specific branding unless requested
- output a small summary of what changed
