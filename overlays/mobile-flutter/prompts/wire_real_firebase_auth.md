# Prompt: Wire Real Firebase Auth

Upgrade the starter auth module to real Firebase Authentication.

Requirements:
- initialize Firebase in bootstrap
- use firebase_auth package in datasource only
- expose AppUser in domain
- keep go_router integration clean
- add sign out and current user refresh flow
- add tests where possible for mapping and repository behavior
