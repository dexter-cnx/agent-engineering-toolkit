# Worked Example — Node Service Feature

Use the foundation lifecycle, then map it to:
- `src/routes/` for transport
- `src/services/` for orchestration
- `src/repositories/` for persistence
- `src/adapters/` for provider boundaries

Verification examples:
```bash
npm run lint
npm test
```
