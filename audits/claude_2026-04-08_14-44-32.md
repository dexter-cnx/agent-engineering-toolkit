# Audit: agent-engineering-toolkit
**Auditor:** Claude Sonnet 4.6
**Date:** 2026-04-08 14:44
**Scope:** Public, production-ready foundation toolkit audit
**Branch:** develop
**Reference tree:** `docs/tree-manifest.txt`
**Prior audit reconciled:** `claude_2026-04-08_14-34-43.md`

---

## 1. Executive Summary

This pass closes every medium and minor issue from the prior audit. The changes are surgical and correct:

- `prompts/audit_repo.md` fully restored as a role-based prompt with all required fields — the single medium regression from the previous pass is resolved.
- All three thin overlay worked examples now include all 7 lifecycle steps (Steps 3 and 6 added) and carry an explanatory header note.
- CHANGELOG disclaimer restored to honest same-day language.
- Both audit entry points (`prompts/audit_repo.md` and `docs/strict-audit-prompt.md`) are now cross-referenced correctly in `docs/strict-audit-prompt.md`, both tutorials, and both how-to guides.
- Tree manifest, release notes, project memory, and push guide all updated.

**Two minor housekeeping issues remain**, both inside `project_memory/` — stale content that was not cleaned up when decisions were reversed in a prior pass. Neither issue affects any adopter.

**Verdict: Ready.** This is the first pass that clears all medium issues and leaves only internal housekeeping items. The public surface of the toolkit is consistent, complete, and adoption-ready.

---

## 2. Critical Issues

None.

---

## 3. Medium Issues

None. The sole medium issue from the prior audit (`prompts/audit_repo.md` gutted) is fully resolved.

---

## 4. Minor Issues

### m1 — `project_memory/decisions.md` contains a directly contradicting stale decision

**File:** `project_memory/decisions.md`

**Problem:** Decision 4 (written when `audit_repo.md` was converted to a stub) states:
```
Decision: `prompts/audit_repo.md` is a compatibility alias, not a second canonical audit prompt.
Consequence: `docs/strict-audit-prompt.md` remains the single canonical audit source.
```

Decision 8 (written when the role-based prompt was restored) states:
```
Decision: `prompts/audit_repo.md` is the role-based audit prompt and `docs/strict-audit-prompt.md` is the invocation template companion.
```

These directly contradict each other. Decision 4 is stale. Any AI agent or maintainer reading project memory gets conflicting instructions about the same file. Decision 4 was never removed when Decision 8 superseded it.

**Fix:** Remove Decision 4 entirely. Decision 8 is the current accurate state.

---

### m2 — `project_memory/known_constraints.md` line 4 is stale and now inaccurate

**File:** `project_memory/known_constraints.md`

**Problem:** Constraint 4 states:
```
- The audit prompt has one canonical source: `docs/strict-audit-prompt.md`.
```

This was written when `audit_repo.md` was a stub redirect pointing to `docs/strict-audit-prompt.md`. The current state has two distinct audit surfaces: `prompts/audit_repo.md` (role-based prompt for AI agents) and `docs/strict-audit-prompt.md` (paste-ready invocation template for humans). Calling one of them "the" canonical source is no longer accurate.

Constraint 8 (`known_constraints.md` line 10) correctly states the current state:
```
- The audit workflow now has two distinct surfaces: the role-based prompt and the invocation template companion.
```

Constraint 4 and Constraint 8 are now in tension.

**Fix:** Remove Constraint 4. Constraint 8 is the current accurate statement.

---

## 5. File-by-File Review

### `AGENTS.md`
Unchanged. No violations. Canonical references accurate.

### `README.md`
Updated: Canonical references section now correctly lists:
- `Audit prompt (role-based): prompts/audit_repo.md`
- `Audit prompt (invocation template): docs/strict-audit-prompt.md`

No violations. No issues.

### `README_TH.md`
Updated: Mirrors English README changes. No issues.

### `CHANGELOG.md`
Updated: Note now reads "v1.0.3 and v1.0.4 were both released on 2026-04-08 during the same working session. Earlier bootstrap iterations are preserved in `docs/releases/`." v1.0.4 entry added with accurate change list.

One factual gap: v1.0.2 existed in the prior release history but has no entry in either the CHANGELOG or `docs/releases/`. The note implies all earlier iterations are in `docs/releases/`, but v1.0.2 is absent from both. This is a minor documentation gap but does not affect adoption. v1.0.1 has a release doc; v1.0.3 and v1.0.4 are in both the CHANGELOG and docs/releases/. The gap is v1.0.2 only.

### `LICENSE`
Unchanged. Complete. No issues.

### `CONTRIBUTING.md`
Unchanged. No issues.

### `CODE_OF_CONDUCT.md`
Unchanged. No issues.

### `SECURITY.md` and `.github/SECURITY.md`
Unchanged from prior pass. Both complete. No issues.

### `.github/CODEOWNERS`
Unchanged. No issues.

### `.github/ISSUE_TEMPLATE/`, `pull_request_template.md`, `workflows/`
Unchanged. No issues.

### `docs/strict-audit-prompt.md`
Updated: now includes "Canonical companion: `prompts/audit_repo.md`" at the top, establishing the two-file relationship explicitly. The prompt content itself is unchanged. No issues.

### `docs/how-to-use.md`
Updated: Section 10 now explicitly mentions both audit paths — role-based and invocation template — with a one-line description of each. Clear and accurate. No issues.

### `docs/how-to-use_TH.md`
Updated: Section 11 mirrors the English Section 10 update in Thai. Accurate. No issues.

### `docs/tutorial.md`
Updated: Step 11 ("Audit") added after the 10 lifecycle-mapped steps. It correctly points to `prompts/audit_repo.md` for agent-style audits and `docs/strict-audit-prompt.md` for paste-ready prompts. Clear distinction. No issues.

Note: The tutorial now has 11 steps but only 10 of them (Steps 4–10 mapping to the 7 lifecycle phases, Steps 1–3 for setup) are lifecycle steps. Step 11 is an optional self-audit step that does not correspond to a lifecycle phase. This is clearly useful but a reader counting "lifecycle steps" may need to understand that Step 11 is additive, not part of the canonical 7-stage lifecycle. The content makes this implicit (it is framed as a tool for auditing, not a workflow stage). Acceptable as-is; minor wording improvement possible but not required.

### `docs/tutorial_TH.md`
Unchanged. Mirrors the English tutorial from prior pass. The Step 11 addition appears to have been made in the English tutorial this pass — checking: the tutorial_TH.md is listed as changed. Both tutorials include Step 11 consistently. No issues.

### `docs/prompt-pipeline.md`, `docs/architecture.md`, `docs/agent-team-system.md`, `docs/overlays.md`
Unchanged. No issues.

### `docs/real-world-integration.md`
Unchanged from prior pass. No issues.

### `docs/repo-bootstrap.md`, `docs/public-repo-checklist.md`, `docs/release-process.md`
Unchanged from prior pass. No issues.

### `docs/roadmap.md`
Unchanged. No issues.

### `docs/tree-manifest.txt`
Updated: includes `audits/claude_2026-04-08_14-34-43.md`, `audits/codex_2026-04-08_14-40-52.md`, and `docs/releases/release-v1.0.4.md`. Accurate to current state.

Note: `release-v1.0.2.md` is absent from both the manifest and the filesystem — consistent with the CHANGELOG gap noted above.

### `docs/releases/release-v1.0.4.md`
New file. Accurately describes focus areas (restoring role-based prompt, explaining overlay lifecycle steps, aligning entry-point docs). No issues.

### `prompts/audit_repo.md`
Restored to full role-based prompt structure: Role, Goal, When to use, Inputs, Process, Required output, Non-goals, See also. Consistent with all other prompts in `prompts/`. Cross-references `docs/strict-audit-prompt.md` in "See also." Prior M1 fully resolved. No issues.

### `prompts/` (all 10 other files)
Unchanged. Consistent role-based structure across all 11 prompts. No issues.

### `agent_team/` (all 7 role files + README)
Unchanged. No issues.

### `core/` (all quick-reference files + README)
Unchanged. `skill-router.md` correctly routes "repo structure problem → repo-audit," which now leads to a fully functional role-based prompt. No issues.

### `skills/` (all 9 READMEs + checklist)
Unchanged. No issues.

### `overlays/mobile-flutter/AGENTS.overlay.md`
Unchanged. 5 Flutter-specific boundary rules. No issues.

### `overlays/mobile-flutter/examples/worked_example.md`
Updated: Added explanatory header note ("This example highlights the key overlay-specific stages..."). Added Step 3 (Implement) and Step 6 (Finalize) as abbreviated but complete entries. All 7 lifecycle stages now present. Prior m1 resolved. No issues.

### `overlays/backend-node/AGENTS.overlay.md`
Unchanged. 5 Node-specific boundary rules. No issues.

### `overlays/backend-node/examples/worked_example.md`
Updated: Same pattern as Flutter — header note added, Steps 3 and 6 added. All 7 stages present. Prior m1 resolved. No issues.

### `overlays/python-service/AGENTS.overlay.md` and `examples/`
Unchanged. Complete. Remains the benchmark overlay. No issues.

### `overlays/web-frontend/AGENTS.overlay.md`
Unchanged. 5 web-specific boundary rules. No issues.

### `overlays/web-frontend/examples/worked_example.md`
Updated: Same pattern — header note, Steps 3 and 6 added. All 7 stages present. Prior m1 resolved. No issues.

### `examples/worked_examples/foundation_feature_flow.md`
Unchanged. Stack-agnostic. All 7 stages. No issues.

### `examples/workflows/` and `examples/prompts/`
Unchanged. No issues.

### `templates/` (all files)
Unchanged from prior pass. No issues.

### `scripts/bootstrap-project-memory.sh`
Unchanged. Functional. URL guard in place. Clear scope messaging. No issues.

### `scripts/check-public-repo.sh` and `check-public-repo.paths`
Unchanged. Correct. No issues.

### `scripts/push-guide.md`
Updated: "create a tag such as `v1.0.4`" replaces the prior v1.0.2 reference. Current and accurate. No issues.

### `scripts/push-new-repo.sh`
Unchanged. No issues.

### `project_memory/decisions.md`
Updated: Decision 8 added (role-based/invocation template split). However, Decision 4 (stale — states `audit_repo.md` is a compatibility alias) was not removed. Contradicting decisions now coexist. See m1.

### `project_memory/known_constraints.md`
Updated: Constraint 8 added (two distinct audit surfaces). Constraint 4 (stale — states one canonical audit source) was not removed. Contradicting constraints coexist. See m2.

### `project_memory/patterns.md`
Updated: Two new patterns added ("Keep the audit role prompt and invocation template distinct" and "Explain skipped lifecycle steps in overlay examples"). Both are accurate and appropriate. Patterns section is now clean — no contradictions. No issues.

---

## 6. Foundation vs Overlay Violations

None detected.

- Root contains no stack-specific assumptions.
- Overlay AGENTS files contain stack-specific rules.
- All worked examples using stack directory names are inside overlay directories.
- `docs/how-to-use_TH.md` Section 7 names "Claude Code" in the role-flow sequence — appropriate (describes a workflow, not a stack assumption).

---

## 7. Public Repo Readiness Assessment

| Requirement | Status | Notes |
|---|---|---|
| `README.md` | Pass | Audit prompts correctly split and labeled |
| `README_TH.md` | Pass | Mirrors English |
| `AGENTS.md` | Pass | No violations |
| `LICENSE` | Pass | Complete MIT |
| `CONTRIBUTING.md` | Pass | No issues |
| `CHANGELOG.md` | Pass | v1.0.4 entry present; same-day note honest; minor v1.0.2 gap is internal history |
| `CODE_OF_CONDUCT.md` | Pass | Present |
| `SECURITY.md` + `.github/SECURITY.md` | Pass | Both complete |
| `.github/CODEOWNERS` | Pass | Correct |
| `.github/ISSUE_TEMPLATE/` | Pass | No issues |
| `.github/workflows/` | Pass | CI and release-check functional |
| `docs/strict-audit-prompt.md` | Pass | Tool-agnostic; cross-references role prompt |
| `prompts/audit_repo.md` | Pass | Fully restored; role-based; consistent with all other prompts |
| `docs/tutorial.md` | Pass | 11 steps; covers all 7 lifecycle phases; audit step added |
| `docs/tutorial_TH.md` | Pass | Mirrors English |
| `docs/how-to-use.md` | Pass | Both audit paths referenced correctly |
| `docs/how-to-use_TH.md` | Pass | Mirrors English |
| `docs/real-world-integration.md` | Pass | Duplicate section removed in prior pass |
| `docs/release-process.md` | Pass | Expanded and actionable |
| `docs/repo-bootstrap.md` | Pass | Complete |
| `docs/public-repo-checklist.md` | Pass | Accurate |
| `docs/tree-manifest.txt` | Pass | Current |
| All 4 overlay `AGENTS.overlay.md` | Pass | Stack-specific rules present |
| All 4 overlay worked examples | Pass | All 7 lifecycle stages in all 4 overlays |
| `scripts/bootstrap-project-memory.sh` | Pass | Guarded; clear scope |
| Foundation stack-agnosticism | Pass | No violations |
| No private URLs | Pass | None found |
| `project_memory/decisions.md` | Minor gap | Contradicting stale Decision 4 not removed |
| `project_memory/known_constraints.md` | Minor gap | Contradicting stale Constraint 4 not removed |

---

## 8. Exact Recommended Edits

### Edit 1 — Remove stale Decision 4 from `project_memory/decisions.md` (m1)

**File:** `project_memory/decisions.md`

Remove this entry entirely:
```
- Date: 2026-04-08
  Decision: `prompts/audit_repo.md` is a compatibility alias, not a second canonical audit prompt.
  Why: Prevents duplicated audit guidance from drifting over time.
  Consequence: `docs/strict-audit-prompt.md` remains the single canonical audit source.
```

Decision 8 is the current accurate state and fully covers this topic.

---

### Edit 2 — Remove stale Constraint 4 from `project_memory/known_constraints.md` (m2)

**File:** `project_memory/known_constraints.md`

Remove this line:
```
- The audit prompt has one canonical source: `docs/strict-audit-prompt.md`.
```

Constraint 8 ("The audit workflow now has two distinct surfaces: the role-based prompt and the invocation template companion.") is the current accurate statement and replaces it.

---

### Edit 3 — Optional: document the v1.0.2 gap in CHANGELOG (housekeeping)

**File:** `CHANGELOG.md`

Add a note in the top disclaimer or in the [Unreleased] section explaining that v1.0.2 was an intermediate iteration that was consolidated into v1.0.3 and has no dedicated release note:

```markdown
> Note: v1.0.2 was an intermediate iteration consolidated into v1.0.3. No separate release note exists.
```

This is not required for adoption but closes the version history gap that would be visible to a contributor checking release continuity.

---

## 9. Final Verdict

**Ready.**

This is the first pass to clear all critical and medium issues across the full audit history. The two remaining items are internal project-memory housekeeping (stale entries that were not cleaned up when decisions were reversed). Neither item is visible to external adopters, creates broken references, or violates the foundation/overlay principle.

The toolkit now satisfies all public-release readiness criteria:

- All required public-repo files are present and current.
- CI passes with no broken paths.
- No private URLs or hardcoded version pins in public-facing files.
- Foundation layer has no stack-specific assumptions.
- All four overlays have differentiated governance rules.
- All four overlay worked examples cover the full 7-stage lifecycle.
- Both audit entry points are properly separated, cross-referenced, and documented.
- Tutorials provide a complete, concrete lifecycle walkthrough.
- Project memory is substantive and (apart from the two stale entries) accurate.
- Release process, versioning guidance, and bootstrap documentation are complete.

Apply Edits 1 and 2 to clean the project memory, and the repo is clean end-to-end.
