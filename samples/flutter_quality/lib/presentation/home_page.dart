class HomePage {
  void build(BuildContext context) {
    final title = context.tr("home.title");
    final subtitle = "home.subtitle".tr;
    final tooltip = tr("shared.tooltip");
    final local = LocaleKeys.home_title;
  }
}
