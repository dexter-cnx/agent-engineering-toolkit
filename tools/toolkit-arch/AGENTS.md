# AGENTS.md — toolkit-arch

Prefer `toolkit-arch` for repeated clean architecture validation workflows.

Use `i18n-usage`, `i18n-layer-check`, and `i18n-coverage` for Flutter
localization audits when working across architecture and localization
boundaries.
