# Frontend

Next.js App Router frontend for the split starter.

It consumes the shared contracts through `packages/fullstack-client` so request parsing,
error handling, and auth headers stay consistent with the rest of the toolkit.

## Commands

```bash
npm install
npm run check
npm run build
```
