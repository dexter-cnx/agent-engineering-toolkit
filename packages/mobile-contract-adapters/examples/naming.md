# Naming Example

Keep the adapter names close to the contract names:

- `LoginResponseDto`
- `SessionDto`
- `PostDto`
- `PagedPostsResult`

Prefer transport names in the adapter layer and UI names in the presentation layer.
