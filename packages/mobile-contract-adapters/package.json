{
  "name": "@agent-toolkit/mobile-contract-adapters",
  "private": true,
  "version": "1.0.0",
  "description": "Mobile-side mapping guidance for shared backend contracts.",
  "scripts": {
    "check": "node ./scripts/check.mjs",
    "lint": "npm run check",
    "test": "npm run check"
  }
}
