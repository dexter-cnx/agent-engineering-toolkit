# SKILLS_INDEX

This package does not replace the existing skill catalog already present in the repository.
Use this file as a consolidation note:

- keep the current `skills/` directory from the repo
- use the Phase 21 docs here as the canonical onboarding + policy layer
- route work through the existing coordinator skills already present in the overlay
