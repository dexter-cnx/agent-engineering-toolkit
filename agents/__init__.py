"""Karpathy Layer V2 — Agent package.

Each agent is a self-contained class that receives a JSON-serialisable
dict payload and returns a JSON-serialisable dict result.  Agents must
not read from or write to disk directly; all I/O is mediated by runners.
"""
