name: Overlay Governance

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  overlays:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python3 tools/ci/overlay_lint.py
