# Example Feature Request

## Scenario
A consuming repository wants to add an account preferences capability while preserving clear boundaries between entry-layer code, business orchestration, persistence, and external integrations.

## Example request to an AI coding tool
Follow AGENTS.md strictly.

Task:
Add an account preferences capability in the consuming repository.

Use the canonical lifecycle from `docs/prompt-pipeline.md`.

Expected qualities:
- assumptions are explicit
- architecture boundaries are named
- implementation follows the agreed structure
- review identifies blocking vs non-blocking issues
- verification records real checks and uncertainty
- durable decisions are written to project memory
