# Task Contract

Every non-trivial task must produce:
- assumptions and scope boundaries
- implementation changes aligned to canonical contracts
- verification evidence (commands + outcomes)
- explicit risks, known gaps, and next-pass actions

A task is incomplete if it changes behavior without updating related docs/policies/memory.
