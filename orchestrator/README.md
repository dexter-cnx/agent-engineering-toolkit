# orchestrator/ (Legacy Prototype)

Status: **legacy orchestration prototype** retained for historical/integration reference.

Canonical repository operating model is documentation and policy driven (`docs/`, `system/`, `agents/`, `prompts/`, `memory/`), not this Python prototype.

Do not treat this directory as the current primary orchestration runtime.
